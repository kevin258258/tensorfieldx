---
title: "测试"
description: "这是一个用于测试排版、公式渲染以及侧边注记组件的模板文件。"
pubDate: 2026-01-01
tags: ["Math", "Physics", "Test"]
---

import Sidenote from '../../components/Sidenote.astro';

## 1. 文本排版测试

这是一段正文内容。我们使用了 **粗体** 来强调重点，以及 `单行代码` 来表示变量。

这里演示如何使用侧边注记：
这是一个关于度规张量的推导过程<Sidenote>这里的度规张量 $g_{\mu\nu}$ 采用的是 $(-+++)$ 符号约定。</Sidenote>。如你所见，在宽屏下它会出现在右侧，手机端则会折叠在下方。

## 2. 数学公式测试

行内公式测试：欧拉公式 $e^{i\pi} + 1 = 0$。

块级公式测试（麦克斯韦方程组）：

$$
\begin{aligned}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\varepsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0\left(\mathbf{J} + \varepsilon_0 \frac{\partial \mathbf{E}}{\partial t}\right)
\end{aligned}
$$

## 3. 代码块测试

```python
def tensor_field_init():
    print("Initializing TensorFieldX System...")
    return True